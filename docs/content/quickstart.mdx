---
title: Quickstart
description: Install infra-ts, create an infra.ts file, and preview the live reconciliation plan.
sidebar:
  label: Quickstart
  order: 1
---

<Steps>
  <Step title="Install or run the CLI">
    Install globally to get the `infra` command.

    ```bash
    npm i -g infra-ts
    infra --help
    # or run ad hoc, without installing
    npx infra-ts --help
    ```

    `infra` and `infra-ts` are the same CLI — use whichever you prefer.

  </Step>
  <Step title="Create infra.ts">
    Declare resources as typed entity instances. An `Account` holds the org/team scope you provision into; entities reference it (and each other) through refs, so wiring creates dependency edges automatically.

    ```ts
    import { defineInfra } from "infra-ts";
    import { NeonAccount, NeonPostgres, NeonProject } from "infra-ts/neon";
    import { VercelProject } from "infra-ts/vercel";

    const neon = new NeonAccount({ name: "personal" });
    const project = new NeonProject({ name: "web", region: "aws-us-east-1", org: neon.id });
    const db = new NeonPostgres({ name: "db", projectId: project.id });

    export default defineInfra({
      entities: [
        neon,
        project,
        db,
        new VercelProject({
          name: "web",
          env: { DATABASE_URL: db.env.databaseUrl },
        }),
      ],
    });
    ```

  </Step>
  <Step title="Authenticate and link accounts">
    `login` runs each provider's OAuth flow through the CLIs you already use (`neonctl`, `vercel`). `link` then lets you pick the org/team for each account and records it in `.infra.<env>`.

    ```bash
    infra login   # OAuth via the provider CLIs
    infra link    # pick an org/team per account → .infra.<env>
    ```

    Prefer explicit credentials in CI? Set them in the environment instead:

    ```bash
    export NEON_API_KEY=...
    export VERCEL_TOKEN=...
    ```

  </Step>
  <Step title="Preview and apply">
    `plan` reads live remote state and shows changes without mutating anything.

    ```bash
    infra plan
    infra apply
    infra status
    ```

  </Step>
</Steps>

:::tip
The first `apply` writes `.infra.<env>`, a tiny identity link file. Keep it local and uncommitted, just like `.env.local` or `.vercel`.
:::

## Pull typed environment values

Entities can expose typed environment outputs. `checkout` pulls those values from live resources and writes `.env.<env>`.

```bash
infra checkout --env local
```

In application code, `parseEnv` validates the process environment without network access:

```ts
import infra from "./infra";
import { parseEnv } from "infra-ts";

const env = parseEnv(infra);
env["db"].databaseUrl;
```

## Run with resolved env

`infra run` checks out env values in memory and runs a child command with those variables injected.

```bash
infra run -- npm run dev
```
